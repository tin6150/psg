
#### apt stuff
#### should be mostly idempotent, so can just run on all instance of wsl2 (or other ubuntu-based machine)


sudo apt update

sudo apt install  gnome-text-editor -y   # was the recommended thing from learn.microsoft.com
# gnome-terminal is currently gabbled up cuz bad graphics driver...
sudo apt install mousepad  # xfce lightweight editor, 3MB

sudo apt install nautilus  -y
# there is vlc, gimp, google-chrome, microsoft-edge

sudo apt install x11-apps -y   # xcalc , xcloc, xeye.  there are display update error.  may need vGPU driver...

sudo apt install xterm 





