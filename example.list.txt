

github container registry ghcr - looked for metabolic:4.0, but never deployed.

github action build docker container
but doesn't push to hub.docker.com ?
see r4eta .github/workflows/docker-image.yml
