

config nodes by John for Asus A40 GPU nodes to boot with uefi

help -b  
-b for break, a pause before scrolling off

ifconfig
mode ?
tftp
ping
reconnect
drivers
set
mode

help ifconfig -b


ifconfig -r # renew config
ifconfig -l # list config

ifconfig -l eth0 

exit

